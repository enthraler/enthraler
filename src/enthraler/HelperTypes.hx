package enthraler;

/**
The type should be a URL.

Currently we just treat this as a String, in future we might add validation and utility methods.
**/
typedef Url = String;

/**
The type should be a [Semver String][1].

Currently we just treat this as a String, in future we might add validation and utility methods.

[1]: http://semver.org/
**/
typedef SemverString = String;
